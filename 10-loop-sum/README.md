# 第 10 題：迴圈的應用（累加計算）

結合 `for` 迴圈與變數，將數字依序累加起來

## 題目說明

1. 建立一個變數 `sum`，初始值為 `0`
2. 寫一個 `for` 迴圈，讓數字從 `1` 跑到 `100`
3. 在迴圈中，將每一次跑到的數字累加進 `sum` 變數裡
4. 迴圈結束後，使用 `console.log()` 印出 `sum` 的最終總和（預期結果為 5050）

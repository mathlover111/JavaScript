# 第 17 題：物件（Object）基礎

學習使用物件，用Key-Value的方式來描述現實生活中的具體事物

## 題目說明

1. 建立一個名為 `user` 的物件
2. 該物件必須包含三個屬性：
   - `name`：賦予字串 `"糰子牛奶"`
   - `age`：賦予數字 `20`
   - `isAdmin`：賦予布林值 `false`
3. 使用 `console.log()` 印出這個物件的 `name` 屬性

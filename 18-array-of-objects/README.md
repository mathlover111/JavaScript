# 第 18 題：陣列與物件的組合運用

在實際開發中，最常遇到的資料結構就是陣列裡面包物件。

## 題目說明

1. 利用現有的 `products` 陣列（裡面包含兩個商品物件）。
2. 使用 `console.log()` 印出**第一個商品**的 **price** 屬性。
3. 使用 `console.log()` 印出**第二個商品**的 **name** 屬性。

# 第 14 題：帶有參數與回傳值的函式

讓函式接收外部傳進來的參數，並回傳值

## 題目說明
1. 宣告一個名為 `multiply` 的函式，並設計兩個參數 `num1` 與 `num2`
2. 在函式內部，使用 `return` 關鍵字回傳這兩個參數相乘的結果
3. 在函式外部呼叫 `multiply(5, 6)`，並用一個新變數 `result` 接收
4. 使用 `console.log()` 印出 `result`（預期結果為 30）
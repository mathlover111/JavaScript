# 第 12 題：FizzBuzz

結合迴圈與條件判斷（if...else），挑戰程式設計中最經典的邏輯思維題。

## 題目說明
寫一個 `for` 迴圈跑數字 `1` 到 `15`，並遵守以下規則印出結果：
1. 如果數字同時是 `3` 和 `5` 的倍數，印出 `FizzBuzz`
2. 如果數字只是 `3` 的倍數，印出 `Fizz`
3. 如果數字只是 `5` 的倍數，印出 `Buzz`
4. 如果都不是，直接印出該數字
# 第 16 題：陣列常用方法 (push & pop)

學習如何動態地在陣列的尾端新增或刪除資料。

## 題目說明

1. 利用現有的 `todoList` 陣列（初始包含 `"讀書"`、`"玩原神"`）
2. 使用陣列方法在尾端新增一項任務：`"倒垃圾"`
3. 使用陣列方法刪除陣列尾端的最後一項任務
4. 使用 `console.log()` 印出最終的 `todoList` 陣列
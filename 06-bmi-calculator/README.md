# 第 6 題：計算 BMI

將前面學到的變數、數字運算與樣板字面量融會貫通，做出一個 BMI 計算器

## 題目說明

1. 利用 `weight`（體重，公斤）與 `height`（身高，公尺）
2. 建立一個新變數 `bmi`，計算公式為：體重 / (身高 * 身高)
3. 建立一個新變數 `result`，組合成一句話：`您的 BMI 為：XX`
4. 使用 `console.log()` 印出 `result`

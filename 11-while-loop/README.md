# 第 11 題：while 迴圈與 break

使用 `while` 迴圈，並利用 `break` 在特定條件下提早強制跳出迴圈。

## 題目說明

1. 建一個變數 `count`，初始值為 `1`
2. 建一個 `while (true)` 的無窮迴圈
3. 在迴圈內，使用 `console.log()` 印出 `count`
4. 每次印出後， `count` 加 1
5. 寫 `if` 判斷式：當 `count` 大於 5 時，執行 `break` 停止迴圈
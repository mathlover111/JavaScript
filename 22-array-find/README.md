# 第 22 題：陣列的高階方法：find 尋找

使用 `find` 在陣列中搜尋，並「抓出第一個」符合條件的元素

## 題目說明

1. 利用現有的 `users` 陣列
2. 使用 `users.find()` 方法，尋找 **`id` 為 2** 的物件
3. 將找到的物件存入變數 `targetUser` 
4. 使用 `console.log()` 印出 `targetUser`

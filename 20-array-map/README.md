# 第 20 題：陣列的高階方法：map 轉換

學習如何使用 `map` 遍歷整個陣列，並將資料進行運算轉換，組合成一個全新、長度相同的陣列

## 題目說明
1. 利用現有的 `prices`
2. 使用 `prices.map()` 方法，將裡面的每個價格都**打八折**
3. 將 `map` 處理完後回傳的新陣列存入變報 `discountedPrices` 
4. 使用 `console.log()` 印出 `discountedPrices`（結果為 `[ 80, 160, 240 ]`）
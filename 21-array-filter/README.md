# 第 21 題：陣列的高階方法：filter 篩選

學習使用 `filter` 檢查整個陣列，將符合特定條件的資料挑選出來，組合成一個全新的陣列

## 題目說明

1. 利用現有的 `ages` 陣列（一組年齡數據）
2. 使用 `ages.filter()` 方法，篩選出**大於或等於 18 歲**的所有年齡
3. 將篩選出來的新陣列存入變數 `adultAges` 
4. 使用 `console.log()` 印出 `adultAges`（結果為 `[ 20, 18, 32 ]`）
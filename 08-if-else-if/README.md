# 第 8 題：多重條件判斷 (if...else if)

學習使用 `else if` 與 `else` 來處理三種或以上的不同狀況

## 題目說明

1. 利用現有的 `score`（分數）變數
2. 寫一個多重條件判斷：
   - 如果 `score` 大於或等於 90，印出 `優等`
   - 如果 `score` 大於或等於 60 且小於 90，印出 `及格`
   - 以上條件都不滿足（小於 60），則印出 `不及格`
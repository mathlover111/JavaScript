# 第 15 題：陣列（Array）基礎

學習使用陣列來儲存一系列按順序排列的資料，並學會如何讀取它

## 題目說明

1. 建立一個名為 `fruits` 的陣列，裡面包含三個字串：`"草莓"`、`"香蕉"`、`"西瓜"`。
2. 使用 `console.log()` 印出這個陣列的**第一個元素**。
3. 使用 `console.log()` 印出這個陣列的**長度**（總共有幾個元素）。

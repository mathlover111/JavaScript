# 第 25 題：展開運算子 (Spread Operator)

學習使用三個點 `...` 複製陣列或物件，合併資料

## 題目說明

1. 利用 `fruits1` 與 `fruits2` 兩個陣列。
2. 使用**展開運算子**（`...`），將這兩個陣列的內容合併到一個全新的陣列 `allFruits` 
3. 使用 `console.log()` 印出 `allFruits`
